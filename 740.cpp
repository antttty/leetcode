#include <vector>
#include <iostream>
using namespace std;

/*删除并获得点数*/
/*对一个数 可以删或者不删 转化为打家劫舍*/

int deleteAndEarn(vector<int>& nums) {
    int ret = 0;
    
    return ret;
}

int main(){
    int data[] = {3, 4, 2};
    vector<int> vecs (data, data + sizeof(data)/sizeof(data[0]));
    int ret = deleteAndEarn(vecs);
    cout << "ret :" << ret << '\n';
    return 0;
}